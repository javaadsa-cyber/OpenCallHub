# 复制为 .env 后按需修改（所有默认值可直接用）

# MySQL root 密码（与 och-api 配置一致即可）
MYSQL_ROOT_PASSWORD=123456

# 宿主机对外 IP：外部 FreeSWITCH 能路由到本机的地址（SIP Contact 头使用）。
# FS 与本机同机则可用 127.0.0.1；FS 在局域网其他机器则填本机局域网 IP，如 192.168.1.100
OCH_HOST_IP=192.168.1.100
